spring:
  application:
    name: CART-SERVICES
  data:
    mongodb:
      authentication-database: admin
      database: cart-services
      host: localhost
      port: 27017
      username: root
      password: password
      auto-index-creation: true
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
server:
  port: 0
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka